{
  "name": "arc-example-ts",
  "version": "1.0.0",
  "scripts": {
    "build": "npx tsc",
    "start": "npm run build && npx arc sandbox",
    "deploy": "npm run build && npx arc deploy"
  },
  "dependencies": {
    "typescript": "^3.6.4",
    "@architect/architect": "^6.0.21"
  }
}
